{
  "name": "@espontaneo/api",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "node --experimental-strip-types --watch src/server.ts",
    "migrar": "node --experimental-strip-types scripts/migrar.ts",
    "semear": "node --experimental-strip-types scripts/semear.ts",
    "tipos": "kysely-codegen --dialect postgres --out-file src/db/tipos-gerados.ts",
    "checar": "tsc --noEmit"
  },
  "dependencies": {
    "@espontaneo/shared": "workspace:*",
    "@fastify/cookie": "^11.0.1",
    "@fastify/cors": "^10.0.1",
    "@fastify/rate-limit": "^10.2.1",
    "argon2": "^0.41.1",
    "fastify": "^5.1.0",
    "kysely": "^0.27.4",
    "pg": "^8.13.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "@types/pg": "^8.11.10",
    "kysely-codegen": "^0.17.0",
    "typescript": "^5.6.3"
  }
}
