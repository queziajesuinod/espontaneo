{
  "name": "espontaneo",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --parallel --filter ./apps/* dev",
    "banco:up": "docker compose -f docker-compose.dev.yml up -d",
    "migrar": "pnpm --filter @espontaneo/api migrar",
    "semear": "pnpm --filter @espontaneo/api semear",
    "tipos": "pnpm --filter @espontaneo/api tipos"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  },
  "packageManager": "pnpm@9.12.0"
}
